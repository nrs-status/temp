let
#  pkgs = import <nixpkgs> {
#    system = "x86_64-linux";
#    overlays = [
#      (self: super: {
#        customCall = "hello";
#      })
#    ];
#  };
  pkgs2 = import <nixpkgs> {};
in
pkgs2.lib.filesystem.packagesFromDirectoryRecursive {
  inherit (pkgs2) callPackage;
  directory = ./.;
}
